# CLI Reference

> Generated from `packages/runtime/src/cli-catalogue.ts`. Do not edit this file by hand.

The `--home <path>` option selects a runtime home and may be placed anywhere in the command. Commands that do not need runtime state omit it.

| Command | Purpose |
| --- | --- |
| `q1x help` | Return the machine-readable command catalogue. |
| `q1x init` | Initialise/open runtime state and return status. |
| `q1x status [--programme <id>]` | Return runtime or programme status. |
| `q1x doctor [--json]` | Run local runtime, connector and desktop diagnostics. |
| `q1x discover --manifest <path>` | Run capability discovery manifests. |
| `q1x limits show` | Show effective runtime limits and weakened overrides. |
| `q1x backup create --output <dir>` | Create a verified SQLite runtime backup. |
| `q1x migration inspect` | Inspect runtime-state migration requirements without mutation. |
| `q1x migration compatibility` | Report whether runtime state is compatible or explicitly migratable. |
| `q1x migration dry-run [--backup <path>]` | Plan runtime-state migration without applying changes. |
| `q1x migration apply [--backup <path>]` | Apply explicit runtime-state migration and record audit evidence. |
| `q1x backup verify <backup-path>` | Verify backup hashes, schema and SQLite integrity. |
| `q1x backup restore <backup-path>` | Restore a verified backup into an empty runtime home. |
| `q1x connectors list` | List built-in connector profiles. |
| `q1x connectors show <id>` | Show one built-in connector profile. |
| `q1x connectors add <id>` | Add a connector to local configuration. |
| `q1x connectors configure <id> [--parameter key=value] [--environment name=ENV_KEY]` | Configure connector parameters and environment-key mappings. |
| `q1x connectors test <id>` | Run connector preflight without executing provider work. |
| `q1x connectors enable <id>` | Enable a configured connector. |
| `q1x connectors disable <id>` | Disable a configured connector. |
| `q1x connectors apply <id>` | Materialise an enabled connector into a runtime endpoint. |
| `q1x capabilities put --file <json>` | Persist a capability descriptor. |
| `q1x capabilities list` | List known capabilities. |
| `q1x capabilities get <id>` | Get one capability descriptor. |
| `q1x bindings put --file <json>` | Persist an execution binding. |
| `q1x bindings list` | List execution bindings. |
| `q1x bindings get <id>` | Get one execution binding. |
| `q1x team form <programme-id> --policy <json>` | Form a capability-routed team for ready work. |
| `q1x team assignments <programme-id>` | List programme work assignments. |
| `q1x team plans <programme-id>` | List programme team plans. |
| `q1x assignment cancel <assignment-id>` | Cancel a planned assignment durably. |
| `q1x proposal validate --file <json>` | Validate a programme proposal without applying it. |
| `q1x proposal accept --file <json>` | Accept a validated proposal with checkpoint protection. |
| `q1x supervision cycle <programme-id> --policy <json>` | Run one bounded supervision cycle. |
| `q1x supervision run <programme-id> --policy <json> [--max-cycles <n>]` | Supervise until an explicit stop condition. |
| `q1x supervision cycles <programme-id> --policy <json>` | List supervision-cycle records. |
| `q1x approval request --file <json>` | Create a fail-closed approval request. |
| `q1x approval list [--programme <id>]` | List approval records. |
| `q1x approval show <id>` | Show one approval record. |
| `q1x approval decide <id> --file <json>` | Approve or reject a pending approval. |
| `q1x approval apply <id>` | Consume an approved authorisation and unblock protected work. |
| `q1x evidence record --file <json> [--programme <id>]` | Record immutable evidence. |
| `q1x evidence list [--programme <id>]` | List evidence records. |
| `q1x evidence show <id>` | Show one evidence record. |
| `q1x audit list` | List tamper-evident audit receipts. |
| `q1x audit verify` | Verify the local audit hash chain. |
| `q1x recovery reconcile [--programme <id>]` | Reconcile interrupted assignments after restart. |
| `q1x adapters list` | List adapter manifests. |
| `q1x adapters get <id>` | Get one adapter manifest. |
| `q1x adapter-endpoints put --file <json>` | Persist an adapter endpoint. |
| `q1x adapter-endpoints list` | List adapter endpoints. |
| `q1x adapter-endpoints get <id>` | Get one adapter endpoint. |
| `q1x adapter execute <endpoint-id> --file <json>` | Execute a request through an adapter endpoint. |
| `q1x adapter discover <endpoint-id>` | Discover capabilities exposed by an adapter endpoint. |
| `q1x browser-endpoints put --file <json>` | Persist a browser endpoint. |
| `q1x browser-endpoints list` | List browser endpoints. |
| `q1x browser-endpoints get <id>` | Get one browser endpoint. |
| `q1x browser run <endpoint-id> --file <json>` | Run a browser action batch. |
| `q1x browser discover <endpoint-id>` | Register browser-control capability evidence. |
| `q1x desktop-endpoints put --file <json>` | Persist a desktop endpoint. |
| `q1x desktop-endpoints list` | List desktop endpoints. |
| `q1x desktop-endpoints get <id>` | Get one desktop endpoint. |
| `q1x desktop setup-first-party [--id <id>] [--name <name>] [--output-dir <dir>]` | Configure the first-party desktop bridge for the host OS. |
| `q1x desktop run <endpoint-id> --file <json>` | Run a desktop action batch. |
| `q1x desktop discover <endpoint-id>` | Register desktop-control capability evidence. |
| `q1x endpoints put --file <json>` | Persist a model endpoint. |
| `q1x endpoints list` | List model endpoints. |
| `q1x endpoints get <id>` | Get one model endpoint. |
| `q1x model invoke --file <json>` | Invoke a configured model endpoint. |
| `q1x mission put --file <json>` | Persist a mission. |
| `q1x mission list` | List missions. |
| `q1x mission show <id>` | Show one mission. |
| `q1x programme put --file <json>` | Persist a programme. |
| `q1x programme list` | List programmes. |
| `q1x programme show <id>` | Show one programme. |
| `q1x graph put --file <json>` | Persist a work graph. |
| `q1x graph show <id>` | Show one work graph. |
| `q1x replan record --file <json>` | Record a replan event. |
| `q1x execution request --file <json>` | Record an execution request. |
| `q1x execution result --file <json>` | Record an execution result. |
| `q1x checkpoint create <programme-id> [--id <id>]` | Create a durable programme checkpoint. |
| `q1x checkpoint list [--programme <id>]` | List checkpoints. |
| `q1x checkpoint restore <id>` | Restore a programme checkpoint. |

## Runtime-home safety

Use a dedicated runtime home for each independent environment. Back up before upgrades or destructive recovery work.
`backup restore` requires an empty target home and fails closed rather than merging state.

## Output contract

Successful CLI commands emit one JSON value to standard output. Errors emit a JSON error object to standard error and return a non-zero exit code.
Do not place secrets in command arguments or persisted endpoint files; use environment-key mappings where supported.
